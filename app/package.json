{
  "name": "go2japan-app",
  "version": "0.2.0",
  "private": true,
  "main": "index.js",
  "description": "Go2Japan 웹앱을 WebView 로 감싼 안드로이드/iOS 앱",
  "scripts": {
    "sync": "node scripts/sync-web.mjs",
    "build:web": "npm --prefix .. run build:app",
    "start": "npm run build:web && expo start",
    "android": "npm run build:web && expo run:android",
    "ios": "npm run build:web && expo run:ios",
    "apk": "npm run build:web && eas build --platform android --profile preview",
    "aab": "npm run build:web && eas build --platform android --profile production",
    "doctor": "expo-doctor",
    "eas-build-pre-install": "cd .. && npm ci && npm run build:app"
  },
  "dependencies": {
    "expo": "^57.0.9",
    "expo-splash-screen": "~57.0.5",
    "expo-status-bar": "~57.0.1",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-native": "0.86.2",
    "react-native-safe-area-context": "~5.7.0",
    "react-native-webview": "13.16.1"
  }
}
